spring:
  application:
    name: song-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

#server:
#  forward-headers-strategy: framework

spring-doc:
  api-docs:
    path: /song-docs
  cache:
    disabled: true